{
  "name": "bugsy",
  "version": "1.0.0",
  "scripts": {
    "prev": "npm install && npm install airtable && cd frontend && npm install && cd ../backend && npm install",
    "start": "cd backend && node index.js"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.16.1",
    "uuid": "^9.0.1"
  }
}
